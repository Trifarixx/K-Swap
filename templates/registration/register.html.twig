{% extends 'base.html.twig' %}

{% block title %}KSwipe - Inscription
{% endblock %}

{% block body %}
	<div
		class="auth-container">

		{# Carte centrale contenant le formulaire #}
		<div
			class="auth-card">

			{# en-tête de la carte #}
			<div class="auth-icon-wrapper">
				<span class="material-symbols-rounded icon-primary">album</span>
			</div>
			<h1 class="auth-title">Bienvenue</h1>
			<p class="auth-subtitle">Crée ton compte et rejoins la communauté.</p>

			{# Formulaire Symfony #}
			{# Affichage des erreurs globales du formulaire (ex: Erreur CSRF) #}
			{{ form_errors(registrationForm) }}

			{# Génère la balise <form> avec la bonne méthode (POST) et l'action #}
			{{ form_start(registrationForm) }}

			{# Champ : Pseudo #}
			<div
				class="form-group">
				{# Affichage du label personnalisé #}
				{{ form_label(registrationForm.pseudonyme, 'Ton Pseudo', {'label_attr': {'class': 'form-label'}}) }}

				{# Affichage de l'input en injectant directement nos classes CSS et un placeholder via Twig #}
				{{ form_widget(registrationForm.pseudonyme, {'attr': {'class': 'form-input', 'placeholder': 'Ex: Twice4Life'}}) }}

				{# Affichage des erreurs spécifiques à ce champ #}
				<div class="form-error">
					{{ form_errors(registrationForm.pseudonyme) }}
				</div>
			</div>

			{# Champ : Email #}
			<div class="form-group">
				{{ form_label(registrationForm.email, 'Ton Email', {'label_attr': {'class': 'form-label'}}) }}
				{{ form_widget(registrationForm.email, {'attr': {'class': 'form-input', 'placeholder': 'Ex: fan@kpop.com'}}) }}
				<div class="form-error">
					{{ form_errors(registrationForm.email) }}
				</div>
			</div>

			{# Champ : Mot de passe #}
			<div class="form-group">
				{{ form_label(registrationForm.plainPassword, 'Mot de passe', {'label_attr': {'class': 'form-label'}}) }}
				{{ form_widget(registrationForm.plainPassword, {'attr': {'class': 'form-input', 'placeholder': '••••••••'}}) }}
				<div class="form-error">
					{{ form_errors(registrationForm.plainPassword) }}
				</div>
			</div>

			{# Champ : Confirmation du mot de passe #}
			<div
				class="form-group">
				{# On englobe le widget dans un label pour que cliquer sur le texte coche la case #}
				<label class="custom-checkbox">
					{{ form_widget(registrationForm.agreeTerms) }}
					<span>J'accepte les conditions d'utilisation</span>
				</label>
				{# Ajout de la zone d'erreur au cas où l'utilisateur oublie de cocher la case #}
				<div class="form-error">
					{{ form_errors(registrationForm.agreeTerms) }}
				</div>
			</div>

			{# Bouton de soumission du formulaire #}
			<button type="submit" class="btn btn-primary auth-submit-btn">
				Créer mon compte
			</button>

			{# Génère les champs cachés restants (le fameux token CSRF de sécurité) et ferme la balise </form> #}
			{{ form_end(registrationForm) }}

			{# Redirection vers la connexion #}
			<div class="auth-footer">
				Déjà membre ?
				<a href="{{ path('app_login') }}" class="auth-link">Se connecter</a>
			</div>

		</div>
	</div>
{% endblock %}
