{% extends 'base.html.twig' %}

{% block title %}Rejoindre K-Swap{% endblock %}


{% block body %}
<div class="auth-container">
    <div class="auth-card">
        <div style="text-align: center; margin-bottom: 1rem;">
            <span class="material-symbols-rounded" style="font-size: 48px; color: var(--primary);">album</span>
        </div>

        <h1 class="auth-title">Bienvenue</h1>
        <p class="auth-subtitle">Crée ton compte et rejoins la communauté.</p>

        {{ form_errors(registrationForm) }}

        {{ form_start(registrationForm) }}
            
            <div class="form-group">
                {{ form_label(registrationForm.pseudonyme, 'Ton Pseudo', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(registrationForm.pseudonyme, {'attr': {'class': 'form-input', 'placeholder': 'Ex: Stay_Forever'}}) }}
                <div style="color: #ef4444; font-size: 0.85rem; margin-top: 0.25rem;">
                    {{ form_errors(registrationForm.pseudonyme) }}
                </div>
            </div>

            <div class="form-group">
                {{ form_label(registrationForm.email, 'Ton Email', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(registrationForm.email, {'attr': {'class': 'form-input', 'placeholder': 'Ex: fan@kpop.com'}}) }}
                <div style="color: #ef4444; font-size: 0.85rem; margin-top: 0.25rem;">
                    {{ form_errors(registrationForm.email) }}
                </div>
            </div>

            <div class="form-group">
                {{ form_label(registrationForm.plainPassword, 'Mot de passe', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(registrationForm.plainPassword, {'attr': {'class': 'form-input', 'placeholder': '••••••••'}}) }}
                <div style="color: #ef4444; font-size: 0.85rem; margin-top: 0.25rem;">
                    {{ form_errors(registrationForm.plainPassword) }}
                </div>
            </div>

            <div class="form-group">
                 <label class="custom-checkbox">
                    {{ form_widget(registrationForm.agreeTerms) }}
                    <span>J'accepte les conditions d'utilisation</span>
                 </label>
            </div>

            <button type="submit" class="btn btn-primary" style="width: 100%; margin-top: 1rem;">
                Créer mon compte
            </button>

        {{ form_end(registrationForm) }}

        <div class="auth-footer">
            Déjà membre ? <a href="{{ path('app_login') }}" class="auth-link">Se connecter</a>
        </div>
    </div>
</div>
{% endblock %}